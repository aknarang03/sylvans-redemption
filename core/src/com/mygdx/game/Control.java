package com.mygdx.game;

// note- can potentially use this to have further controller support

public enum Control { // what key presses can mean
    UP, DOWN, LEFT, RIGHT, POSSESS, SELECT, PAUSE, RESTART
}
